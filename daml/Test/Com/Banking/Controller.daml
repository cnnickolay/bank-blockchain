daml 1.2

module Test.Com.Banking.Controller where

import Main.Com.Banking.Controller

template instance ControllerA = Controller A
instance Common A

test : Scenario ()
test = do
    owner <- getParty "party"
    let z = Controller A with owner, value = 1.0
    submit owner do
        create z
        create A with owner, value = 1.0
    pure ()