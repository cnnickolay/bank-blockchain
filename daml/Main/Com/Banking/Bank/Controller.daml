daml 1.2

module Main.Com.Banking.Bank.Controller where

import Main.Com.Banking.Account.Service qualified as Account
import Main.Com.Banking.Bank.Model qualified as Bank

template Controller
  with
    creator: Party
    bankOwner: Party
    bankId: Bank.K
  where
    signatory creator

    key (creator, bankId) : (Party,  Bank.K)
    maintainer key._1

    controller bankOwner can
      nonconsuming AccountRequest : Account.Response
        with command : Account.Request
        do Account.handle bankOwner command
